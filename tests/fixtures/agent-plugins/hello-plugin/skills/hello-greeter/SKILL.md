---
name: hello-greeter
description: Fixture skill from the hello-plugin Agent Plugin; greets the user by name.
---

# Hello Greeter

This skill ships inside the `hello-plugin` Agent Plugin fixture
(`tests/fixtures/agent-plugins/hello-plugin`). When invoked, greet the user
warmly and mention that this greeting came from an Agent Plugin skill.
